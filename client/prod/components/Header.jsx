import React from 'react';
import ReactDOM from 'react-dom';

const Header = (props) => {
  return (
    <div>
      Hello Header! I'm gonna take the Profile Component
    </div>
  );
};

export default Header;